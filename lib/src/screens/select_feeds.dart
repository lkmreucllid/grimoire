import 'package:flutter/material.dart';

class SelectFeed extends StatelessWidget {
  const SelectFeed({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
